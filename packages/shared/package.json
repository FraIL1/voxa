{
  "name": "@voxa/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Общие типы, константы и zod-схемы Voxa (используются сервером и клиентами)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
